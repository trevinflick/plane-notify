version: "3.9"
services:
    plane-notify:
        platform: linux/amd64
        shm_size: 2gb
        restart: unless-stopped
        build:
            context: .
        volumes:
            - ./:/plane-notify
